- [ ] make "Save on format" and `prettier --write` have the same behavior

## Development

To run both the discourse graph extension and Next.js website in development mode:

```bash
turbo run dev
```
