import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "command-palette": "Command palette",
  "bulk-identify-discourse-nodes": "Bulk identify nodes",
  "sync-and-import": "Sync and import",
};

export default meta;
