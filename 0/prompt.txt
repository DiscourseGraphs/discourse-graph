I still have to negotiate this, but let's start with the obvious: Using roamAlphaAPI.file.get in fetchAssetBytes . Thinking: Should we return just the bytes there, or the whole File object? I'm thinking the metadata in that call _may_ be more authoritative than the one we got from fetchAssetDescriptor, which could be used as an approximation. In particular, it's likely that the first will report the encrypted size and not the decrypted size, which could make some shortcuts less reliable.