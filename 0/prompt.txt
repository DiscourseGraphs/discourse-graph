On the last point: Makes sense, and yes I can live with that. Thank you for confirming my understanding. I may want to revisit the decomposition, after all. We are leaning towards moving things to firebase, and that means we need a registry. Trying to think of the registry shape. When importing another page, we want the registry keyed by hash, to reuse the url. I'm thinking... if we reimport the page, and an asset reference was deleted, how do we garbage collect in supabase? In theory I could go through the URLs in the previous page's version, and see which ones are targets of a block. A bit expensive going through the hash in reverse, but probably not that bad with a filter, right?