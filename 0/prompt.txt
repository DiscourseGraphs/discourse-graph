Some of these require thought. Actually... not sending the token makes sense; we don't want to give access to the URL, only to use it as an identifier. Access will be through Supabase and its RLS. Agree a that URL-only fallback is not a reference. BUT not sending the token means that Obsidian cannot count on the metadata to get the original filename, and that's slightly problematic. There is another decision point here: We could send it as a fake query parameter, but that's a lot of fragile convention. A downside of Obsidian having it at all is that we may end up with filename collisions; but Obsidian does handle filename collisions iirc. The other option is to use the generated id in the firebase URL, which would look like nothing, but won't collide. Please check my reasoning.