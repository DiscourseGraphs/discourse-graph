First, you're right to push back. But I want to think deeper. Using the hash (vs file contents) in CrossAppAsset makes perfect sense for the import path. We may not need the file content. And indeed the publish path in Obsidian, and soon in Roam, starts with addFile and then creates the CrossAppAssets with the resulting hashes for upsert. We may one day consider a utility function that receivesa CrossAppConcept with... CrossAppAssetInput? that would have the file object instead of the hash; and call addFile internally. But that does not seem like a useful abstraction yet, am I right?