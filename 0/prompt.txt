Ok. Let's think again about how to do this. I'd separate this in the following: 1. A note that mentions that some scope was moved to ENG-2247. 2. Any change to Solution or Done when that is just a move can be done as direct edits, because the original intent is kept, just moved, and the delta is heavy to read otherwise. 3. Additional "Done when" is just extra constraints, also direct edits. 4. Scope expansions (such as title collision) need a note justifying the expansion, with the usual pattern. No direct edit, even if we do it in the PR. Did I cover everything? And if so how would it look?