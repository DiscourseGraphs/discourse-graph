First, I think you over-interpreted the phrasing change: We will allow the URL-only fallback when bytes are not available. But good call. Second, I think we can put the firebase URL in filepath to avoid a database change, ugly as that is. But there is no case where we'd have both a URL and a filepath, so the proper thing to do after v0 may be to just rename that field to path_or_url. Does that sound right? Let's clear that before dealing with the pivot.