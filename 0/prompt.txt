Ok, the node was imported but deleted. Ah, I think I understand what happened. The ResourceAccess is missing. I did the earlier test in the test database instead of my normal dev database. So the ResourceAccess was put there, and the node had a publishedToGroups entries, so it was not recreated in the dev database. Deleting that entry allows the test to pass. 3.1 can be marked done. Moving on.