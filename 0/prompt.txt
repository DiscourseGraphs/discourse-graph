Ah, I'll do that. (I'll wind down soon.) Thinking about the assets: Right now, reading nodes goes through the SharedNode type, but that is a historical artifact and should go through CrossAppNode. You may notice that CrossAppNode is missing in dbToCrossAppConverters, that will be a refactoring at some point. When that function is written, I expect it will do the my_file_references query and populate the field accordingly.