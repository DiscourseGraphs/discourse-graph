BTW, I cannot argue that ENG-2229 shipped, it still has to be approved. One bit of context about it being a temporary hack: At some point we'll transform the markdown text to atjson before storage. He thinks that will reduce the need for two columns, but I think he's wrong. The atjson record for the text anchor will have to be some file identifier. We could use the hash but that introduces unnecessary coupling (we need to change the atjson if the file is modified.) So we probably still need two values (identifier+filename).