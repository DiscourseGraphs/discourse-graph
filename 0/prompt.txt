ok. Tried editing a previously published node, with the image in another folder. There are many links to the same image to test various Obsidian linking strategies. Since they coincide, this maps to a single FileReference. When the sync triggered, the FileReference did not gain a source_path. In the network transactions, I see a GET of my_file_references, then a single DELETE of a FileReference; but the FileReference is still there, unfortunately untouched.