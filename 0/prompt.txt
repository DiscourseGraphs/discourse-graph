Ok. In the design, there is an option we eliminated too fast. I think it's wrong, but if I'm right we need to say why. When we upload a file in roam, the filename is preserved and can be retrieved with an API call. (see https://roamresearch.com/#/app/developer-documentation/page/O_LlivVFD ) So in theory we could put the fileHash with a "imported-" marker as a filename and retrieve it. What bothers me is that we have to apply this to every file on resync. But maybe that's not that bad. What would this not cover?