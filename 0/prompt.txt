No, I want to preserve the pure split for a human reviewer. The AI reviewer found one last nit, but I think we can let it go, it's preexisting.
One cosmetic residual, not worth blocking on: the fallback name carries no extension, so in that pathological case (a source_path of only dots or only characters sanitizeFileName strips) the asset lands extensionless and Obsidian will not render the embed inline, though the link resolves and the bytes are intact. Giving it one would mean selecting mimetype in fetchFileReferences (:459), which currently fetches only filepath, filehash, source_path, created, last_modified.