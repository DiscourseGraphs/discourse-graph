I think the future split is unlikely, so let's go with MAX_ASSET_BYTES. Fair point that it's technically about destination platforms, and keep it in the comment, but I also don't expect platforms that are destination-only in the near future (unless you count supabase as a platform, which makes some sense, but it's also not strictly destination-only.) So yes, otherwise go ahead with suggestions.