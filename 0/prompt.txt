ok, while checking 3.1 I ran into an unrelated bug, I'd like you to check the code. I was trying to import from space B into space A. The problem is that A never looks at space B, and does not show it in the import panel. They have a common group, but all network queries to my_spaces are constrained with a list of ids, and there is no unconstrained query (which I've verified would work.) This worked before; and space B is in the spaceNames of A's data.json. How is the list of queried spaces decided?